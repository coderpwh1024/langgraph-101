竖版 3:4 手绘教育信息图（sketch-notes 卡通手绘风格），与封面同一手绘风格、同一配色，保持系列一致。

【整体风格】手绘抖动线条，暖奶油色纸张背景 (#F5F0E8) 带纸纹；马卡龙配色彩色圆角卡片，珊瑚红仅强调；填色不填满边缘；手写体中文，深炭灰文字。

【标题】顶部手写大标题：「三个维度 · 别再混了」，标题旁一个小小的困惑表情😵，下方一条珊瑚红手绘下划线。标题下方一行手写小字：「三件事是三个维度，不是包含关系」。

【主体内容】dense 布局，分上下两块：

上半部分「三张并排竖卡」，三个概念各占一张彩色圆角卡片。每张卡片顶部只写一个英文单词，单词必须完整连写、拼写完全正确，绝对不要在字母之间加连字符或空格，也不要画任何拆开的字母序列：
1. 英文「checkpointer」+ 中文「存档员」—— 把整个 state 拍快照存档 —— 下方一行：管的是「会不会丢」（手绘相机📷图标；卡片底部两行小字：内存版=临时 / 落盘版=持久）
2. 英文「thread_id」+ 中文「存档编号」—— 决定读哪一份存档 —— 下方一行：管的是「读谁的会话」（手绘书签/号码牌图标）
3. 英文「namespace」+ 中文「抽屉标签」—— store 里按用户分抽屉 —— 下方一行：管的是「谁的长期记忆」（手绘贴标签的抽屉图标）
这三个英文单词是画面里仅有的英文（除 state、store 外），每张卡里中文短句不要重复出现两次。

下半部分「本块唯一难点」提示框：一个珊瑚红手绘虚线框，框角贴一个手绘感叹号⚠标签，里面画一条简短的双格对比：
- 左格：没有存档员（画一个打叉的相机）→ 同一个存档编号也读不到上次的文件 —— 因为根本没存档
- 右格：换了存档编号（画两个不同号码牌）→ 也读不到 —— 因为读错了存档
下方一行手写结论，珊瑚红强调关键词：「先存档（会不会丢）→ 再选编号（读谁的）；两个都对才读得到上一轮」。

【装饰】角落点缀手绘小星星和波浪线。不要渲染颜色名称或色值作为可见文字，不要把本提示词里的说明文字画进图里。
