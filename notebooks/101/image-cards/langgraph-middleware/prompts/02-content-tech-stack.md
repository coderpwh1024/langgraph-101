竖版 3:4 手绘教育信息图（sketch-notes 卡通手绘风格），与封面同一手绘风格、同一配色，保持系列一致。

【整体风格】手绘抖动线条，暖奶油色纸张背景 (#F5F0E8) 带纸纹；马卡龙配色彩色圆角卡片（淡蓝/薰衣草紫/薄荷绿/蜜桃橙交替），珊瑚红仅强调；填色不填满边缘；手写体中文，深炭灰文字。

【标题】顶部手写大标题：「核心技术栈 · 9 件套」，下方一条珊瑚红手绘下划线。

【主体内容】dense 高密度布局，严格 3 行 × 3 列 = 正好 9 个圆角彩色小卡片（不多不少，禁止重复任何一项），每个卡片一个手绘小图标 + 名称 + 一句话作用：
1. create_agent —— 快速构建 ReAct 智能体（小积木图标）
2. @tool —— 把函数变成可调用工具（扳手图标）
3. interrupt() —— 暂停执行等人工（暂停图标）
4. Command(resume) —— 从断点恢复并注入决策（回弹箭头图标）
5. checkpointer —— 状态持久化存档（存档桶图标）
6. thread_id —— 会话隔离标识（标签图标）
7. AgentMiddleware —— 生命周期插入逻辑（齿轮图标）
8. @dynamic_prompt —— 动态生成系统提示词（魔法棒图标）
9. context_schema —— 运行时上下文驱动（人物角色图标）

【装饰】角落点缀手绘小星星和波浪线。不要渲染颜色名称或色值作为可见文字。
