# 03 内容 · 双目录树对照

竖版 3:4 手绘知识卡片，sketch-notes 风格，与系列同风格同吉祥物。

**画面**：
- 米色纸张背景，顶部标题："📁 两边的抽屉各放什么？"
- 主体左右分栏对照（中间一条手绘虚线分隔，顶部各有一个标签页）：
- 左栏（马卡龙蓝底色区）标签"用户级 ~"，手绘目录树：
  - `.codex/` → `config.toml`（基础配置）、`AGENTS.md`（全局指令）、
    `prompts/`（已废弃 🪦）、`hooks.json`、`rules/`
  - `.agents/skills/`（个人技能 ✨）
- 右栏（薄荷绿底色区）标签"项目级 <repo>"，手绘目录树：
  - `.codex/` → `config.toml`（项目覆盖）、`agents/`、`hooks.json`、`rules/`，
    旁边小闸机图标"⚠️ trusted 才加载"
  - `.agents/skills/`（项目技能，无 trusted 要求）
  - `AGENTS.md`（项目指令，子目录可嵌套）
- 两栏的 `.agents/skills/` 都用珊瑚色（#E8655A）手绘圆圈圈出，
  中间飘一张便签："⭐ 技能在 .agents/ 不在 .codex/！"
- 小吉祥物在底部举着放大镜

**风格约束**：马卡龙配色、中文手写卡通体、目录名等宽感英文、
路径文字必须准确无误、整体对称平衡。
